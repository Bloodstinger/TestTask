package com.example.demo.utils;

/**
 * Since node type is predefined, it is better to use enum for it's typification
 */
public enum NodeType {
    NETWORK, SUBSTATION, TRANSFORMER, FEEDER, RESOURCE;
}